---
description: 快速触发文章优化技能。用法：/optimize [文章链接或文件路径]
argument-hint: [文章链接或本地文件路径]
---

调用 `optimize-article` 技能，对以下目标进行文章优化：

**输入目标：** $ARGUMENTS

请按照技能定义的完整工作流执行：获取内容 → 分析诊断 → 应用优化原则 → 输出报告 → 确认保存。
